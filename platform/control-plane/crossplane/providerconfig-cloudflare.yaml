apiVersion: cloudflare.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: cloudflare
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane
      name: cloudflare-credentials
      key: credentials
